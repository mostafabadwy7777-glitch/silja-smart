name: Build Android APK (EAS)

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read
  actions: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Delete old build artifacts (APKs) to free up storage
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Fetching list of existing artifacts..."
          IDS=$(gh api "repos/${{ github.repository }}/actions/artifacts?per_page=100" \
            --jq '.artifacts[].id')

          if [ -z "$IDS" ]; then
            echo "No old artifacts found — nothing to delete."
          else
            for ID in $IDS; do
              echo "Deleting artifact ID $ID"
              gh api -X DELETE "repos/${{ github.repository }}/actions/artifacts/$ID" || true
            done
          fi

      - name: Checkout
        uses: actions/checkout@v4

      - name: Unzip uploaded project archive
        run: |
          ZIP_FILE=$(find . -maxdepth 1 -iname "*.zip" | head -n1)
          if [ -z "$ZIP_FILE" ]; then
            echo "No .zip file found in repo root. Upload your project zip to the repo root first."
            exit 1
          fi
          echo "Extracting $ZIP_FILE"
          unzip -o "$ZIP_FILE" -d .
          rm -f "$ZIP_FILE"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies & EAS CLI
        run: |
          npm install
          npm install -g eas-cli

      - name: Build on EAS (free tier)
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          for i in {1..5}; do
            echo "Attempt $i: Triggering EAS Build..."
            if eas build \
              --platform android \
              --profile preview \
              --non-interactive \
              --no-wait \
              --json > build-start.json; then
              echo "Build triggered successfully!"
              cat build-start.json
              break
            fi
            echo "GraphQL request failed, retrying in 10 seconds..."
            sleep 10
            if [ $i -eq 5 ]; then
              echo "Failed to start EAS build after 5 attempts."
              exit 1
            fi
          done

      - name: Wait for EAS build and download APK
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          set -euo pipefail
          BUILD_ID=$(node -e "console.log(JSON.parse(require('fs').readFileSync('build-start.json','utf8'))[0].id)")
          echo "Build ID: $BUILD_ID"

          TIMEOUT_SECONDS=$((90 * 60))
          START_TIME=$(date +%s)
          STATUS="unknown"
          i=0

          while true; do
            i=$((i + 1))
            NOW=$(date +%s)
            ELAPSED=$((NOW - START_TIME))

            if [ "$ELAPSED" -ge "$TIMEOUT_SECONDS" ]; then
              echo "Timed out after 90 minutes. Last status: $STATUS"
              exit 1
            fi

            # Fetch status safely without crashing on transient GraphQL/Network errors
            if eas build:view "$BUILD_ID" --json > build-status.json 2>/dev/null; then
              STATUS=$(node -e "try { console.log(JSON.parse(require('fs').readFileSync('build-status.json','utf8')).status.toLowerCase()); } catch(e) { console.log('unknown'); }")
            else
              STATUS="retrying_network"
            fi

            echo "[$i] status: $STATUS (elapsed ${ELAPSED}s)"

            case "$STATUS" in
              finished)
                break
                ;;
              errored|canceled)
                echo "Build failed with status: $STATUS"
                cat build-status.json 2>/dev/null || true
                exit 1
                ;;
              *)
                sleep 30
                ;;
            esac
          done

          ARTIFACT_URL=$(node -e "console.log(JSON.parse(require('fs').readFileSync('build-status.json','utf8')).artifacts.buildUrl)")
          echo "APK URL: $ARTIFACT_URL"
          curl -L "$ARTIFACT_URL" -o app-release.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: silja-smart-release-${{ github.run_number }}
          path: app-release.apk
          retention-days: 7
